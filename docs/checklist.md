# Build Checklist

## Build Preferences

- **Build mode:** Autonomous
- **Comprehension checks:** N/A (autonomous mode)
- **Git:** Commit after each item with message: `Complete step N: [title]`
- **Verification:** No — build straight through, review at the end
- **Check-in cadence:** N/A (autonomous mode)

## Checklist

- [x] **1. Project scaffold + environment setup**
  Spec ref: `spec.md > Stack`, `spec.md > Runtime & Deployment`, `spec.md > File Structure`
  What to build: Create the full folder structure from spec.md > File Structure. Initialize Python backend with `requirements.txt` covering fastapi, uvicorn, langgraph, langgraph-checkpoint-mongodb==0.3.1, langgraph-store-mongodb==0.2.0, pymongo, python-telegram-bot==22.7, apscheduler, openai, langchain-community, playwright, python-dotenv, pydantic. Create `backend/.env` with all required variables: MONGODB_URI, OPENAI_API_KEY, TELEGRAM_BOT_TOKEN, MONGO_DB_NAME=powerposition, INACTIVITY_THRESHOLD_HOURS=48, SESSIONS_BEFORE_PLAN_UPDATE=3. Initialize frontend with `npm create vite@latest frontend -- --template react-ts`. Install frontend deps: tailwindcss v4, shadcn/ui, react-router-dom@7, @tanstack/react-query@5. Create `frontend/tailwind.config.ts` with dark mode enabled and sci-fi color tokens (base: #0a0a1a, indigo/purple atmospheric palette). Create minimal `backend/main.py` (empty FastAPI app) so the server can start. Install Playwright Chromium: `playwright install chromium`.
  Acceptance: `cd backend && uvicorn main:app --reload --port 8000` starts without import errors. `cd frontend && npm run dev` serves a page at localhost:5173.
  Verify: Both startup commands run cleanly with no errors in either terminal.

- [x] **2. MongoDB data layer**
  Spec ref: `spec.md > Data Model`, `spec.md > Backend > FastAPI Application`
  What to build: Create `backend/db/client.py` — MongoDB client using MONGODB_URI from env, expose named collection references for all 7 collections (calendar_entries, sessions, coaching_plans, agent_logs, workflow_state, training_docs, config). Add unique index on `calendar_entries.date`. Programmatically attempt to create Atlas Vector Search index `training_docs_vector_index` on startup (1536 dims, cosine similarity, path: "embedding") — include a note that index creation can take 1-2 minutes. Create `backend/db/models.py` — Pydantic models for all 7 collections matching the exact schemas in spec.md > Data Model.
  Acceptance: Backend starts with MongoDB connected. All 7 collection references are accessible. Vector Search index creation is attempted without errors (even if not yet ready).
  Verify: Backend starts cleanly, no MongoDB connection errors in the log. MongoDB Atlas UI shows the `powerposition` database with all 7 collections present.

- [x] **3. Ingestion pipeline**
  Spec ref: `spec.md > Ingestion Pipeline`
  What to build: Create `backend/ingestion/chunker.py` using LangChain's `RecursiveCharacterTextSplitter` (500-token chunks, 50-token overlap). Returns list of `{content, chunk_index, source_url}`. Create `backend/ingestion/embedder.py` — for each chunk, calls OpenAI `text-embedding-3-small` (1536 dims), upserts document to `training_docs` with fields: content, embedding, source_url, chunk_index, ingested_at. Create `backend/api/documents.py` with `POST /api/documents/upload` accepting file upload, running it through chunker → embedder. On startup (in main.py), call the ingestion pipeline with at least one publicly available ultimate frisbee training resource URL (e.g. a USA Ultimate coaching page or similar) so RAG has content to retrieve from the first run.
  Acceptance: At least 5 documents exist in `training_docs` collection with `embedding` arrays of exactly 1536 floats.
  Verify: Check MongoDB Atlas `training_docs` collection. Confirm 5+ documents with non-null `embedding` field of length 1536.

- [x] **4. LangGraph agent core**
  Spec ref: `spec.md > LangGraph Agent > Graph Definition`, `spec.md > LangGraph Agent > Graph State`, `spec.md > LangGraph Agent > Memory Setup`, `spec.md > LangGraph Agent > Prompts`
  What to build: Create `backend/agent/memory.py` — initialize MongoDBSaver (checkpointer) and MongoDBStore (store) using the shared MongoDB client. Create `backend/agent/prompts.py` — system prompt root (elite ultimate frisbee coach: analytical, direct, improvement-focused). Four tone variants by task_type: pre_game (motivational, brief), post_game (curious, empathetic, data-gathering), session_log (efficient, confirmatory), proactive_checkin (warm, low-pressure). Add a 2-3 question onboarding prompt variant for first /start interaction that captures: position, experience level, goals — stores answers to MongoDBStore. Create `backend/agent/graph.py` — PowerPositionState TypedDict with 6 fields (messages, task_type, question_count, athlete_id, plan_needs_update, last_tool_sources). Define agent_node (calls LLM with task_type-appropriate system prompt) and tools_node (ToolNode). Conditional edge: tool calls → tools_node; task_type=="post_game" and question_count >= 3 → force plan_update tool call; plan_needs_update == True → force plan_update; else → END. Compile with checkpointer and store. Expose `invoke_graph(task_type, athlete_id, message)` helper.
  Acceptance: `from agent.graph import invoke_graph` imports cleanly. Calling `invoke_graph("session_log", "test_user", "hello")` returns a response string.
  Verify: From the backend directory, run `python -c "from agent.graph import invoke_graph; r = invoke_graph('session_log', 'test_user', 'hello'); print(r)"` — confirm a response is returned without errors.

- [x] **5. Agent tools**
  Spec ref: `spec.md > LangGraph Agent > Tools`
  What to build: Create `backend/agent/tools.py` with all 10 tools decorated with `@tool`. Implement each per spec.md > LangGraph Agent > Tools: web_search (Playwright browser → navigate top results → extract text), document_ingest (web_search → chunker → embedder → upsert), rag_retrieve (Atlas Vector Search on training_docs, top-k chunks + source URLs; if index not ready, return helpful message rather than crashing), session_log_write (insert to sessions + update workflow_state.last_activity + increment sessions_since_update; if count >= SESSIONS_BEFORE_PLAN_UPDATE set plan_needs_update=True in state), session_log_read (return N most recent sessions from sessions collection), calendar_write (upsert calendar_entries for the given date), plan_update (upsert coaching_plans with short/medium/long term text + reset sessions_since_update to 0), agent_log_write (write to agent_logs including last_tool_sources from state), cycle_status_update (update workflow_state.cycle_status field), telegram_send (send message via bot instance imported from runner). Import tools list into graph.py and bind to agent_node via `model.bind_tools(tools)`.
  Acceptance: All 10 tools import without errors. `rag_retrieve.invoke({"query": "ultimate frisbee conditioning", "k": 3})` returns results or a graceful empty-index message.
  Verify: Run `python -c "from agent.tools import rag_retrieve; print(rag_retrieve.invoke({'query': 'ultimate frisbee', 'k': 3}))"` from the backend directory.

- [x] **6. FastAPI app + API routes + scheduler**
  Spec ref: `spec.md > Backend > FastAPI Application`, `spec.md > Backend > API Routes`, `spec.md > Backend > Scheduler`
  What to build: Expand `backend/main.py` — full startup sequence: (1) initialize MongoDB client, (2) attempt Vector Search index creation, (3) start APScheduler, (4) start Telegram bot polling as asyncio background task. Add CORS middleware allowing http://localhost:5173. Mount all API routers. Create all 6 API route files: `calendar.py` (GET /api/calendar returns all calendar_entries sorted by date; POST /api/calendar/{date} upserts entry), `plan.py` (GET /api/plan returns latest coaching_plans document), `logs.py` (GET /api/agent-logs returns agent_logs reverse-chronological; GET /api/cycle-status returns current workflow_state.cycle_status), `documents.py` (POST /api/documents/upload — file → ingestion pipeline), `simulate.py` (POST /api/simulate/pregame invokes graph with task_type="pre_game"; POST /api/simulate/postgame invokes with task_type="post_game" and question_count=3 to force plan_update; POST /api/simulate/inactivity calls check_inactivity() directly), `config.py` (GET /api/config returns config doc; POST /api/config/telegram saves token). Create `backend/scheduler.py` — APScheduler BackgroundScheduler checking workflow_state.last_activity every hour, invoking graph with task_type="proactive_checkin" if hours_since > INACTIVITY_THRESHOLD_HOURS.
  Acceptance: All API endpoints return valid JSON. `curl http://localhost:8000/api/calendar` and `curl http://localhost:8000/api/plan` both return 200 with valid (empty) JSON.
  Verify: Start the backend. Run `curl http://localhost:8000/api/calendar`, `curl http://localhost:8000/api/plan`, `curl http://localhost:8000/api/agent-logs`, `curl http://localhost:8000/api/cycle-status` — all return 200.

- [ ] **7. Telegram bot**
  Spec ref: `spec.md > Telegram Bot > Handlers`, `spec.md > Telegram Bot > Bot Runner`
  What to build: Create `backend/bot/handlers.py` with 5 handlers: start_handler (/start → task_type="session_log", seeds MongoDBStore athlete profile using onboarding prompt from prompts.py, stores initial workflow_state document for athlete_id), pregame_handler (/pregame → task_type="pre_game"), postgame_handler (/postgame → task_type="post_game"), log_handler (/log → task_type="session_log"), message_handler (any text → routes to active LangGraph thread using stored task_type for athlete_id). Each handler calls invoke_graph(task_type, athlete_id=str(chat_id), message=text). Create `backend/bot/runner.py` — ApplicationBuilder setup with all 5 handlers registered, `run_polling()` async coroutine. Bot instance must be importable from runner.py for use in telegram_send tool. Wire into main.py startup as `asyncio.create_task(run_bot())`.
  Acceptance: Sending /start to the Telegram bot receives an onboarding response. Sending /log followed by "ran 5km intervals" causes a new document to appear in the MongoDB sessions collection.
  Verify: In Telegram, send /start and confirm onboarding response. Send /log then "did 20 minutes of disc throwing practice". Check MongoDB Atlas sessions collection for the new entry.

- [ ] **8. Frontend scaffold + Landing page**
  Spec ref: `spec.md > Frontend > Landing Page`, `spec.md > Frontend > Navigation`, `spec.md > Stack`
  What to build: Configure the Vite + React + TypeScript project fully: apply Tailwind CSS v4, initialize shadcn/ui. Create `frontend/tailwind.config.ts` with sci-fi dark palette (base bg #0a0a1a, indigo/purple glow accents, custom color tokens). Create `frontend/src/main.tsx` — entry point wrapping App in QueryClientProvider. Create `frontend/src/App.tsx` — React Router v7 routes: / → Landing, /calendar → CalendarPage, /plan → CoachingPlan, /config → Config. Create `frontend/src/components/nav/Sidebar.tsx` — fixed left sidebar (hidden on / route, visible on all others). Three nav items: Calendar (→/calendar), Coaching Plan (→/plan), Config (→/config). Active item highlighted with bright accent against dark bg. Create `frontend/src/pages/Landing.tsx` — full-screen dark bg with CSS radial gradient (deep space feel, indigo/purple atmospheric glow). Centered layout: "PowerPosition" title (large, wide letter-spacing), subtitle describing the product, "Explore" CTA button that navigates to /calendar.
  Acceptance: localhost:5173 shows Landing with deep space aesthetic. Clicking "Explore" navigates to /calendar with sidebar visible and Calendar highlighted.
  Verify: Open localhost:5173, confirm dark space aesthetic, large title, "Explore" button. Click it, confirm URL is /calendar, sidebar is present, Calendar nav item is active.

- [ ] **9. Calendar screen**
  Spec ref: `spec.md > Frontend > Calendar Screen`
  What to build: Create `frontend/src/lib/api.ts` — typed fetch wrapper functions for all API endpoints (getCalendar, saveCalendarEntry, getPlan, getAgentLogs, getCycleStatus, getConfig, connectTelegram, uploadDocument). Create `frontend/src/lib/hooks.ts` — React Query hooks: useCalendar (GET /api/calendar, refetchInterval: 5000), useSaveCalendarEntry (mutation POST /api/calendar/{date}, optimistic update). Create `frontend/src/components/calendar/CalendarGrid.tsx` — custom 7-column CSS grid (no library). Current month display with day headers. Each cell: day number + filled dot indicator if an entry exists for that date. Clicking any cell opens DatePopup for that date. Create `frontend/src/components/calendar/DatePopup.tsx` — modal overlay. Diary-style textarea pre-populated with existing entry content if one exists. Save button triggers useSaveCalendarEntry mutation, closes modal on success. Create `frontend/src/pages/CalendarPage.tsx` — composes CalendarGrid and DatePopup, passes selected date to popup.
  Acceptance: Calendar shows current month grid. Clicking a date opens the popup. Typing and saving text causes a dot to appear on that date. Entry persists on page refresh.
  Verify: Open /calendar. Click any date, type "morning disc drills", click Save. Confirm dot appears on that date. Refresh and confirm entry is still present.

- [ ] **10. Coaching Plan screen**
  Spec ref: `spec.md > Frontend > Coaching Plan Screen`
  What to build: Add to hooks.ts: usePlan (GET /api/plan, refetchInterval: 5000), useLogs (GET /api/agent-logs, refetchInterval: 5000), useCycleStatus (GET /api/cycle-status, refetchInterval: 5000). Create `frontend/src/components/plan/PlanSection.tsx` — displays label (Short-Term / Medium-Term / Long-Term) + agent-generated plan text. Empty state: "Add more session logs to reveal insights". Create `frontend/src/components/plan/CycleStatus.tsx` — pill/badge showing current cycle state. Four states with color coding: "Awaiting next session" (grey), "Session logged" (blue), "Analyzing" (amber), "Plan updated" (green). Create `frontend/src/components/plan/AgentLogsTable.tsx` — reverse-chronological table with 4 columns: Timestamp, Action, Reasoning, Vector Search Sources. Inline checkpoint event rows (e.g. "Workflow resumed from checkpoint"). Empty state when no logs. Create `frontend/src/pages/CoachingPlan.tsx` — composes CycleStatus (top), three PlanSection components (Short/Medium/Long), AgentLogsTable (bottom).
  Acceptance: /plan shows all sections in empty state, CycleStatus shows "Awaiting next session". After triggering POST /api/simulate/postgame, plan sections and logs update within 5 seconds.
  Verify: Open /plan, confirm all empty states. Run `curl -X POST http://localhost:8000/api/simulate/postgame`. Watch the page — confirm plan sections and AgentLogsTable update within 5 seconds.

- [ ] **11. Config screen + simulate endpoints + end-to-end smoke test**
  Spec ref: `spec.md > Frontend > Config Screen`, `spec.md > Backend > API Routes`
  What to build: Create `frontend/src/components/config/TelegramCard.tsx` — if not connected: text input for bot token + "Connect" button (calls POST /api/config/telegram); if connected: green "Connected" badge + bot username display. Create `frontend/src/components/config/IntegrationCard.tsx` — reusable card with integration name, greyed-out toggle (disabled), greyed-out "Connect" button. Visually distinct inactive state. Create `frontend/src/pages/Config.tsx` — TelegramCard + IntegrationCard for Calendar + IntegrationCard for Strava. Ensure backend/api/simulate.py is complete: pregame simulation invokes graph with task_type="pre_game" using a default test athlete_id and sends Telegram message; postgame simulation invokes with task_type="post_game" and question_count=3 to bypass follow-up questions and force plan_update; inactivity calls check_inactivity() directly. Run full end-to-end smoke test: navigate all 4 screens, trigger all 3 simulate endpoints, verify MongoDB collections are populated (sessions, coaching_plans, agent_logs, calendar_entries, workflow_state), verify Telegram messages received for pregame and postgame sims.
  Acceptance: Config screen shows TelegramCard + 2 greyed-out integration cards. All 3 simulate endpoints trigger LangGraph, write to MongoDB, and update the Coaching Plan UI within 5 seconds. Telegram messages received for pregame and postgame.
  Verify: Open /config, confirm layout. Run all 3 simulate curls. Open /plan — confirm plan sections populated, logs populated with Vector Search sources and event types. Check Telegram — confirm pre-game and plan-update messages received.

- [ ] **12. Submit your project to Devpost**
  Spec ref: `prd.md > What We're Building`
  What to build: Walk through the Devpost submission form. Write a project name ("PowerPosition") and tagline. Draft the project story using scope.md and prd.md as source material — explain what you built, why, and what makes the MongoDB usage meaningful (Vector Search + dual memory + document collections all doing real work). Add built-with tags: MongoDB Atlas, LangGraph, FastAPI, React, Python, Telegram, OpenAI. Take screenshots of all 4 screens (Landing, Calendar with entries, Coaching Plan with populated plan + agent logs, Config) plus the Telegram bot conversation. Create a GitHub repo if not already done, push all code, and include the link. Upload the docs/ folder artifacts (scope.md, prd.md, spec.md, checklist.md). Review all fields and submit.
  Acceptance: Submission is live on Devpost with project name, tagline, description, built-with tags, screenshots of all 4 screens, docs artifacts, and repo link.
  Verify: Open your Devpost submission page and confirm the green "Submitted" badge appears. Read the description — does it tell the story clearly to someone who knows nothing about the project?
